# The function's own HTTPS endpoint. The app calls it directly for conversions
# so a long request is not cut short by Hosting's request timeout.
PUBLIC_API_URL=https://us-central1-yt-mp3-57bee.cloudfunctions.net/api
